# Minimal requirements for running the FastAPI + SUMO server
# If you install SUMO via system packages/binaries, you may not need a pip 'traci' package.
fastapi==0.119.0
uvicorn[standard]==0.37.0
sumolib==1.24.0
# Optional: uncomment to install the PyPI traci package if you prefer a pip-provided binding
# traci
